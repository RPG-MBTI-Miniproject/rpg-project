# app.py 가 직접 import 하는 패키지만 적습니다.
# 나머지(click, jinja2, itsdangerous, blinker, markupsafe, pyjwt, dnspython 등)는
# pip 가 알아서 딸려 받습니다.
#
# 버전을 == 로 못 박지 않는 이유:
#   EC2 의 파이썬 버전(3.9 / 3.10 / 3.12 ...)에 따라 설치 가능한 버전이 달라서,
#   정확한 버전을 박아두면 서버에서 pip install 이 실패합니다.
#   실제로 이전 requirements.txt 에는 존재하지 않는 버전이 4개 들어 있었습니다
#   (Flask-JWT-Extended==4.7.4, click==8.4.2, dnspython==2.8.0, python-dotenv==1.2.3).

Flask>=3.0,<4
Flask-JWT-Extended>=4.6,<5
Werkzeug>=3.0,<4
pymongo>=4.6,<5
python-dotenv>=1.0
rapidfuzz>=3.0
